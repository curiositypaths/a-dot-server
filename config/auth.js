// const passport = require("passport");
// const { Strategy: LocalStrategy } = require("passport-local");
// const { User } = require("../models");

// passport.use(
//   new LocalStrategy(function(username, password, done) {
//     throw "Inside Passport middleware";
//   })
// );
